Implementation of J. Molnar, E. Tasnadi et al.: Active surfaces for selective object segmentation in 3D.

CMake build file provided.

The CMake builds a shared library together with the public api in ```include```.

